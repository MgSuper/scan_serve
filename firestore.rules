rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // LOCAL DEV MODE: Allow full read/write access during development
    match /restaurants/{restaurantId}/{document=**} {
      allow read, write: if true;
    }

  }
}