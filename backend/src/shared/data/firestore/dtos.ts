/** Firestore accepts Date values and converts them to Timestamp on write. */
export type FirestoreTimestamp = Date | { toDate(): Date };
export interface FirestoreMetadataDto { createdAt: FirestoreTimestamp; updatedAt: FirestoreTimestamp; submittedAt?: FirestoreTimestamp; acceptedAt?: FirestoreTimestamp; preparedAt?: FirestoreTimestamp; servedAt?: FirestoreTimestamp; deletedAt?: FirestoreTimestamp; deletedBy?: string; isArchived: boolean; }
export interface MenuDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; name: string; description?: string; isActive: boolean; }
export interface CategoryDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; menuId: string; name: string; description?: string; displayOrder: number; isActive: boolean; }
export interface MenuItemDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; menuId: string; categoryId: string; name: string; description?: string; imageUrl?: string; price: number; displayOrder: number; isAvailable: boolean; }
export interface CartItemDto { menuItemId: string; name: string; unitPrice: number; quantity: number; note?: string; modifiers?: string[]; }
export interface CartDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; tableId: string; tableSessionId: string; customerSessionId: string; items: CartItemDto[]; }
export interface OrderItemDto extends FirestoreMetadataDto { id: string; orderId: string; restaurantId: string; branchId: string; menuItemId: string; categoryId: string; name: string; unitPrice: number; quantity: number; note?: string; modifiers?: string[]; }
export interface OrderDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; tableId: string; tableSessionId: string; customerSessionId: string; status: 'PENDING' | 'ACCEPTED' | 'PREPARING' | 'READY' | 'SERVED' | 'CANCELLED'; items: OrderItemDto[]; customerNote?: string; }
export interface TableSessionDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; tableId: string; status: 'ACTIVE' | 'COMPLETED' | 'EXPIRED'; customerCount: number; startedAt: FirestoreTimestamp; endedAt?: FirestoreTimestamp; expiresAt: FirestoreTimestamp; }
export interface CustomerSessionDto extends FirestoreMetadataDto { id: string; restaurantId: string; branchId: string; tableId: string; tableSessionId: string; languageCode?: string; deviceId?: string; status: 'ACTIVE' | 'INACTIVE' | 'EXPIRED'; lastActivityAt: FirestoreTimestamp; expiresAt: FirestoreTimestamp; }
